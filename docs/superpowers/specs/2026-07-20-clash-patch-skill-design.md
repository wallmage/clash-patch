# Clash Patch Skill Design

**首次确定：** 2026-07-20
**当前版本：** 2026-07-22

本文档固定记录产品需求和架构。详细配置规则保存在 `clash-patch/references/patch-policy.md`，开发流程保存在 `AGENTS.md`。

## 产品目标

Clash Patch 面向不懂网络配置的用户，包含两个模块：

- **Patch** 为 macOS ClashX Meta 和 Windows Clash Verge Rev 的 Mihomo 配置增加 DNS、TUN、AI 分流和 UDP/WebRTC 防护。
- **Diagnostics** 接受用户模糊的网络故障描述，主动复现、取证、分析、解释、修复并复测，不要求用户先判断问题类型或指出日志位置。

失败时必须保留原配置并给出中文说明。真实案例只用于检验工作流，不作为固定问题、固定工具或固定答案写进产品。

产品不支持旧版 ClashX、旧版 Clash Verge 或 Linux，不包含 Apple、iCloud、Speedtest 等个人规则。

## 核心要求

- 绝不退出、停止或重启 Clash。
- macOS 修改当前订阅后通过 Mihomo 本地控制器自动刷新并检查；失败时恢复原文件和原运行配置。不得切换 TUN、订阅、代理组或节点。
- 只处理客户端当前存储位置，不扫描旧容器、备份或废弃订阅。
- 保留 ClashX Meta 的默认 `config.yaml`；当前未选中它时安静跳过。
- 已有 AI 分组只补规则并保留原有成员；没有时创建包含全部真实节点和代理提供者的独立 AI 选择器。
- 主代理组与 AI 节点独立选择，让普通流量可走便宜节点、AI 流量可单独走家宽；不创建安全代理分组，也不自动替用户选择节点。
- DNS 按域名和出站拆分：`nameserver-policy` 的 `geosite:cn` 与 `direct-nameserver` 使用大陆 IP DoH 并通过 `DIRECT` 连接；普通国外域名使用带原主代理组标签的 IP DoH；AI 域名使用带 AI 分组标签的 IP DoH。保留节点启动解析。
- 所有 UDP 先经过 AI 分组，节点不支持时拒绝兜底。HTTP/3/QUIC 因此不会直连；国内网页的 TCP 和 DNS 仍按域名直连。这是共享浏览器中避免 WebRTC 直连所需的取舍，不能把浏览器白屏自动归因于 QUIC。
- 不做按应用隔离：浏览器同时承载国内和 AI 网站，STUN 连接也不携带原网页域名，Mihomo 无法判断它来自哪个标签页。TCP 和 DNS 按域名分流，WebRTC 防护覆盖全局 UDP。
- 修改前备份，候选必须可解析、可通过 Mihomo 校验且二次转换一致。
- 不输出订阅地址、认证信息、控制器密钥或完整节点地址。

## Diagnostics 架构

Diagnostics 与 Patch 独立。用户报告网络异常时默认进入只读诊断，不能因为出现 Clash、DNS 或代理字样就运行安装程序。单项 Clash 配置修复仍属于 Diagnostics，并复用 Patch 的安全事务；只有用户明确要求完整安全增强时才进入 Patch。

诊断采用同一个闭环处理不同问题：

1. 把用户描述转成可以复现或监测的现象。
2. 有 Computer Use 且故障能在界面看到时，先在用户实际应用中复现；保存修改前基线，并用异常目标加至少两个健康对照建立范围矩阵。
3. 同时保留多个解释，按区分能力选择浏览器、应用、DNS、Mihomo 连接、传输质量、系统计数和相关日志证据。
4. 用独立证据和反证更新判断；资料不足时建立有期限的观察窗口。
5. 执行最小、可恢复的修复。两次假设或修改无效后恢复全部试验并做诊断重置，没有新证据不进行第三次修改。本机无法处理时，基于已确认的问题类别查询当前官方方案。
6. 在同一应用、账号、目标和动作中连续复测原始症状，并用小白能懂的语言报告结论、证据、修复、结果和未知项。

Skill 不维护一张每次全跑的诊断清单。具体工具由现场决定；完整 DNS/WebRTC 与 AI 分流验收只属于 Patch 或相关故障。macOS 使用实时进程、连接和接口数据，不能虚构不存在的历史；Windows 可以读取系统保存的网络用量，但聚合总量不能冒充逐应用归属。

浏览器诊断必须把主文档网络耗时、关键资源、首次显示和加载结束分开。配置缺陷不能直接当成故障原因。怀疑扩展、过滤器或共同网络组件时，目标开关对照只回答该目标是否受影响；还要用至少两个健康对照判断范围。只有单站异常时修目标交互；跨目标出现同类故障时修共同组件本身，不按网站不断加例外。无改善立即恢复，不能因为浏览器问题顺手修改 Clash。

## 平台架构

### macOS

安装程序单次运行。它只处理 ClashX Meta 当前使用的本地目录或 iCloud 容器，不安装 LaunchAgent、`WatchPaths` 或计划任务。当前订阅写入后自动刷新，清除 Fake-IP 与 DNS 缓存，并确认 TUN、DNS、外网连通性和原有代理组选择均正常；否则立即恢复。

Ruby 引擎使用 YAML 1.2，保护有效的 REALITY `short-id` 文本。每份候选重新解析、再次转换，并由 Mihomo 1.19.27 以上版本校验；校验最长 30 秒。文件通过锁、一次性私有备份和原文件描述符写入，避免覆盖同时刷新的订阅。

### Windows

Clash Verge Rev 通过 `profiles/Script.js` 在订阅加载或刷新时应用同一策略，不安装额外后台服务。

客户端运行时只更新全局脚本；客户端未运行时，PowerShell 安装程序可以事务式更新应用级 TUN 与 IPv6 设置。已有同步 `main` 可以安全组合；结构不明确、异步或递归入口保持不动。

## 生效与验收

当前订阅只有本地控制器自动刷新和运行检查都通过，才能说补丁已经生效。随后先用 Mihomo 实时连接确认国内网站获得大陆 CDN 并走 `DIRECT`、Google 走主代理节点、OpenAI/Anthropic/Claude 走 AI 节点，再检查两项 WebRTC 页面和 DNS 深度测试。有 Computer Use 时由代理完成网页操作；没有时给用户中文步骤并要求返回失败截图。只有分流正确、三项网页都没有红色提示，且没有未代理公网 IP、私网地址或本地运营商 DNS，才能说验证通过。

## 固定文档

- `README.md`：用户介绍、安装、平台机制和限制。
- `clash-patch/SKILL.md`：代理必须执行的流程。
- `clash-patch/references/patch-policy.md`：详细产品规则、异常和状态。
- 本文档：产品目标与架构。
- `AGENTS.md`：开发、测试和发布规则。

不得自行增加新的需求入口或汇总文档。需求变化时直接更新上述固定文件。
